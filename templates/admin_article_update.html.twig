{% extends "base.html.twig" %}
{% block main %}
<div>
    {{ form_start(articleForm) }}

    <div class="title-form-article">
        {{ form_label(articleForm.title, 'Titre', {'label_attr': {'class': 'test'}}) }}
        {{ form_widget(articleForm.title, {'attr': {'class': 'test'}}) }}
    </div>

    <div class="content-form-article">
        {{ form_label(articleForm.content, 'Contenu', {'label_attr': {'class': 'test'}}) }}
        {{ form_widget(articleForm.content, {'attr': {'class': 'test'}}) }}
    </div>

    <div class="image-form-article">
        {{ form_label(articleForm.image, 'Image', {'label_attr': {'class': 'test'}}) }}
        {{ form_widget(articleForm.image, {'attr': {'class': 'test'}}) }}
    </div>

    <div class="createdat-form-article">
        {{ form_label(articleForm.createdAt, 'Date de création', {'label_attr': {'class': 'test'}}) }}
        {{ form_widget(articleForm.createdAt, {'attr': {'class': 'test'}}) }}
    </div>

    <div class="ispublished-form-article">
        {{ form_label(articleForm.isPublished, 'Publiable', {'label_attr': {'class': 'test'}}) }}
        {{ form_widget(articleForm.isPublished, {'attr': {'class': 'test'}}) }}
    </div>

    {{ form_end(articleForm) }}
</div>
{% endblock main %}